node_modules/
dist/
__pycache__/
*.pyc
.astro/
course-template/.build/

# Sandbox mount stubs the agent harness creates inside each project root.
# Not config — character devices. The repo's own .claude/skills/ is tracked.
course-template/.claude/
course-template/.mcp.json
.cc-writes/

# Scratch for generated artefacts (image briefs, model output).
.tmp/

# Vercel CLI project linkage — local state, and a stale one silently redeploys elsewhere.
.vercel/
